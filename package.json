{
  "name": "fiver",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install-server": "npm install --prefix api",
    "install-client": "npm install --prefix client",
    "install": "npm-run-all --sequential install-server install-client",
    "server": "npm run start --prefix api",
    "client": "npm run dev --prefix client",
    "build":"npm run build --prefix client && npm start --prefix api",
    "watch": "npm-run-all --parallel server client",

    "test": "npm run test --prefix api && npm run test --prefix client"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
